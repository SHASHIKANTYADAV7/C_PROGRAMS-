#include<stdio.h>
#include<conio.h>

int main()
{
       char cArr[20] = {'K', 'A', 'B', 'C', 'D'};

       puts(cArr);

       puts("\n Enter A String: ");
       gets(cArr);

       puts("\n Given String Is => ");
       gets(cArr);

       getch();
       return 0;
}
